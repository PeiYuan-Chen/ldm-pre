[project]
name = "depth-anything-v2"
version = "0.1.0"
description = "Depth Anything V2"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "opencv-python",
]

[build-system]
requires = ["uv_build>=0.9.26,<0.10.0"]
build-backend = "uv_build"

[tool.uv.build-backend]
module-name = "depth_anything_v2"
module-root = ""